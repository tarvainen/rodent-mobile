<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="rodent.rodentmobile" >

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".activities.LibraryActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".activities.DrawingActivity"
            android:label="@string/title_activity_drawing" >
        </activity>
        <activity
            android:name=".activities.ManualControllerActivity"
            android:label="@string/title_activity_manual_controller" >
        </activity>
        <activity
            android:name=".activities.BroadcastActivity"
            android:label="@string/title_activity_broadcast" >
        </activity>
        <activity
            android:name=".activities.SplashActivity"
            android:label="@string/title_activity_splash" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
